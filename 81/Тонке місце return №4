Что выведет каждый из echo в результате выполнения следующего кода:

<?php
	function func($num) {
		if ($num <= 0) {
			return abs($num);
		} else {
			return $num * $num;
		}
	}
	
	echo func(10);
	echo func(-5);
?>
Объясните почему.



Виводиться 100 і 5, оскільки тут у функції передається 2 параметри (10 і -5), значення першого параметру 10 підпадає під умову в else, а значення другого
параметру -5 - під першу умову в конструкції if.
